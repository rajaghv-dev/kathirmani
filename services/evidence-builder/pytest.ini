; Local pytest config for the evidence-builder package, so the suite can be run
; from inside this directory (the hyphenated dir name isn't a valid module name;
; the package __init__ tolerates both import styles).
[pytest]
testpaths = tests
python_files = test_*.py
