; Local pytest config so the suite runs from inside services/security too
; (the package is imported via the repo root on sys.path; see tests/conftest.py).
[pytest]
testpaths = tests
python_files = test_*.py
