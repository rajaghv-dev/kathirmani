; Local pytest config for the cv-oss-worker package, so the suite can also be
; run from inside this directory (the hyphenated dir name isn't a valid module
; name; the package __init__ tolerates both import styles).
[pytest]
testpaths = tests
python_files = test_*.py
